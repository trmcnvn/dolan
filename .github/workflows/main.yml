name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: cargo fmt -- --check
      - run: cargo clippy -- -Dwarnings
      - run: cargo test
